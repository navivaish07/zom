#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

int main() {

    pid_t pid;

    pid = fork();

    if(pid > 0) {

        printf("Parent Process\n");
        sleep(10);

    }

    else if(pid == 0) {

        printf("Child Process Exited\n");

    }

    return 0;
}
